# 🧬 Data Model (ERD)

```mermaid
erDiagram
    ARTICLES {
        int id PK
        string title
        string author
        date published_date
        string text_content_path
        string html_content_path
        string summary_one_min
        string summary_one_liner
        int reading_time
        string url_domain
        string url
        date created_at
        int model_id FK
        int execution_time
    }

    TAGS {
        int id PK
        string name
        string[] synonyms_list
    }

    CONCEPTS {
        int id PK
        string name
        string[] synonyms_list
        int parent_id FK
    }

    MODELS {
        int id PK
        string name
        string version
        string description
        int task_type FK
        date created_at
        date last_updated
        string endpoint
        string input_format
        string output_format
        config
    }

    PREDICTIONS {
        int id PK
        int article_id FK
        int model_id FK
        int task_type FK
        string output
        float confidence
        date created_at
        int execution_time
    }

    TASK_TYPES {
        int id PK
        string type_name
    }

    ARTICLE_TAGS {
        int article_id FK
        int tag_id FK
    }

    ARTICLE_CONCEPTS {
        int article_id FK
        int concept_id FK
    }

    CONCEPTS ||--|| CONCEPTS : "Parent-Child"
    ARTICLES ||--o{ ARTICLE_TAGS : "has tags"
    TAGS ||--o{ ARTICLE_TAGS : "tagged in"
    ARTICLES ||--o{ ARTICLE_CONCEPTS : "linked to concepts"
    CONCEPTS ||--o{ ARTICLE_CONCEPTS : "referenced in"
    ARTICLES ||--o{ PREDICTIONS : "has predictions"
    MODELS ||--o{ PREDICTIONS : "generated by"
    MODELS ||--|| TASK_TYPES : "classified as"
    TASK_TYPES ||--o{ PREDICTIONS : "classified as"
```